<style>
/* Service page additions on top of the shared lander styles */
.svc-intro { padding: 2.5rem 0 0; }
.svc-prose { max-width: 42rem; }
.svc-prose p { margin: 0 0 0.9rem; color: var(--ink-soft); font-size: 1.02rem; line-height: 1.65; }
.svc-prose p:last-child { margin-bottom: 0; }

.svc-list { display: grid; gap: 0.7rem; list-style: none; margin: 0; padding: 0; }
.svc-list li {
  background: var(--bg);
  border: 1px solid var(--line);
  border-radius: var(--radius);
  padding: 0.9rem 1.1rem;
  font-size: 0.97rem;
  line-height: 1.6;
}
@media (min-width: 640px) { .svc-list { grid-template-columns: 1fr 1fr; } }
@media (min-width: 900px) { .svc-list { grid-template-columns: repeat(3, 1fr); } }

.svc-faq { max-width: 46rem; }
.faq-item { border-bottom: 1px solid var(--line); }
.faq-item:first-child { border-top: 1px solid var(--line); }
.faq-q {
  width: 100%; background: none; border: 0; cursor: pointer;
  display: flex; align-items: center; justify-content: space-between; gap: 1.25rem;
  padding: 1.1rem 0; text-align: left;
  font-size: 1rem; font-weight: 600; color: var(--ink);
  transition: color 0.15s;
}
.faq-q:hover { color: var(--copper); }
.faq-icon { color: var(--copper); font-size: 1.5rem; line-height: 1; flex-shrink: 0; transition: transform 0.25s; }
.faq-item.open .faq-icon { transform: rotate(45deg); }
.faq-a { max-height: 0; overflow: hidden; transition: max-height 0.3s ease; }
.faq-item.open .faq-a { max-height: 40rem; }
.faq-a-inner { padding: 0 0 1.1rem; color: var(--ink-soft); font-size: 0.97rem; line-height: 1.75; max-width: 44rem; }

#why .focus-card p { margin: 0; color: var(--ink-soft); font-size: 0.95rem; }
#how .step p { margin: 0; color: var(--ink-soft); font-size: 0.95rem; }
</style>
